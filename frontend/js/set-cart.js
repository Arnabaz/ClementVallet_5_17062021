/*
Création d'une instance de classe orderCustomer qui correspond à la commande du client
et qui sera transmise à l'API pour pouvoir renvoyer un n°commande.
 */

let params = new URLSearchParams(document.location.search);
let idProduct = params.get("id");

// Création d'une classe orderCustomer

// Création de l'instance de classe orderCustomer

// Gestion de la commande du client avec le local storage

// Création de l'objet contact qui sera envoyée à l'API